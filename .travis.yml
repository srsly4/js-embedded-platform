sudo: false
dist: trusty
language: c
addons:
  apt:
    sources:
      - sourceline: ppa:team-gcc-arm-embedded/ppa
    update: true
    packages:
      - gcc-arm-none-eabi=7-2018q2-1

env:
 - TARGET_PLATFORM=nucleo-f429zi

script:
  - bash ./.travis/platform-build.sh
